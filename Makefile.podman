.PHONY: help setup install dev dev-backend dev-frontend test lint format clean podman-up podman-down podman-logs devcontainer-open

# Podman configuration
PODMAN := podman
COMPOSE := podman-compose

help:
	@echo "AI Video Editor - Podman & Dev Container Commands"
	@echo "=================================================="
	@echo ""
	@echo "Setup Commands:"
	@echo "  make setup              - Complete project setup (venv + dependencies)"
	@echo "  make install            - Install all dependencies"
	@echo ""
	@echo "Development Commands:"
	@echo "  make dev                - Start all services (Podman)"
	@echo "  make dev-backend        - Start backend only"
	@echo "  make dev-frontend       - Start frontend only"
	@echo ""
	@echo "Podman Commands:"
	@echo "  make podman-up          - Start Podman containers"
	@echo "  make podman-down        - Stop Podman containers"
	@echo "  make podman-logs        - View Podman logs"
	@echo ""
	@echo "Dev Container Commands:"
	@echo "  make devcontainer-open  - Open in dev container (Windsurf)"
	@echo ""
	@echo "Quality Commands:"
	@echo "  make test               - Run all tests"
	@echo "  make lint               - Lint code"
	@echo "  make format             - Format code"
	@echo ""
	@echo "Cleanup Commands:"
	@echo "  make clean              - Remove cache files"

# Setup
setup: install
	@echo "✅ Setup complete!"
	@echo "Next: make dev"

install:
	@echo "📦 Installing dependencies..."
	cd backend && python -m venv venv
	cd backend && .\venv\Scripts\pip install --upgrade pip
	cd backend && .\venv\Scripts\pip install uv
	cd backend && .\venv\Scripts\uv pip install -e ".[dev]"
	cd frontend && npm install
	@echo "✅ Dependencies installed"

# Development
dev: podman-up
	@echo "🚀 All services started with Podman!"
	@echo "Frontend: http://localhost:3000"
	@echo "Backend: http://localhost:8000"
	@echo "API Docs: http://localhost:8000/docs"

dev-backend:
	@echo "🚀 Starting backend..."
	cd backend && .\venv\Scripts\uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

dev-frontend:
	@echo "🚀 Starting frontend..."
	cd frontend && npm run dev

# Podman
podman-up:
	@echo "🐳 Starting Podman containers..."
	$(COMPOSE) -f docker-compose.yml up -d
	@echo "✅ Containers started"

podman-down:
	@echo "🛑 Stopping Podman containers..."
	$(COMPOSE) -f docker-compose.yml down
	@echo "✅ Containers stopped"

podman-logs:
	@echo "📋 Podman logs..."
	$(COMPOSE) -f docker-compose.yml logs -f

podman-ps:
	@echo "📊 Podman containers status..."
	$(COMPOSE) -f docker-compose.yml ps

# Dev Container
devcontainer-open:
	@echo "🔧 Opening in dev container..."
	@echo "Use Windsurf's 'Dev Containers: Reopen in Container' command"
	@echo "Or run: code --remote container-name /path/to/workspace"

# Testing
test:
	@echo "🧪 Running tests..."
	cd backend && .\venv\Scripts\pytest tests/ -v --cov=app

test-backend:
	@echo "🧪 Running backend tests..."
	cd backend && .\venv\Scripts\pytest tests/ -v --cov=app

test-frontend:
	@echo "🧪 Running frontend tests..."
	cd frontend && npm run test

# Code Quality
lint:
	@echo "🔍 Linting code..."
	cd backend && .\venv\Scripts\ruff check app/
	cd frontend && npm run lint

lint-backend:
	@echo "🔍 Linting backend..."
	cd backend && .\venv\Scripts\ruff check app/

lint-frontend:
	@echo "🔍 Linting frontend..."
	cd frontend && npm run lint

format:
	@echo "✨ Formatting code..."
	cd backend && .\venv\Scripts\black app/
	cd frontend && npm run format

format-backend:
	@echo "✨ Formatting backend..."
	cd backend && .\venv\Scripts\black app/

format-frontend:
	@echo "✨ Formatting frontend..."
	cd frontend && npm run format

# Cleanup
clean:
	@echo "🧹 Removing cache files..."
	cd backend && rmdir /s /q __pycache__ 2>nul || true
	cd backend && rmdir /s /q .pytest_cache 2>nul || true
	cd frontend && rmdir /s /q node_modules\.cache 2>nul || true
	@echo "✅ Cleanup complete"

# Status
status:
	@echo "📊 Project Status"
	@echo "================="
	@echo ""
	@echo "Backend:"
	@if exist "backend\venv" (echo "  ✅ Virtual environment exists") else (echo "  ❌ Virtual environment missing")
	@echo ""
	@echo "Frontend:"
	@if exist "frontend\node_modules" (echo "  ✅ Dependencies installed") else (echo "  ❌ Dependencies not installed")
	@echo ""
	@echo "Podman:"
	$(COMPOSE) -f docker-compose.yml ps
